<%= render "layouts/errors", :object => @opinion %>

<%= form_for [@contribution, @opinion] do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br>
  <%= f.label :content %>
  <%= f.text_field :content %>
  <br>
  <br>
  <%= f.submit %>
<% end %>
